export default {
	// populateRequestStateFilter: () => {
		// if (checkboxExcludeCompleted.isChecked) {
			// return Dom_RequestState.view().filter(option => option.label !== "COMPLETED")
		// } else {
			// return Dom_RequestState.view()
		// }
	// }
} 